// Brand image assets. Served from /public so the URLs are stable across dev/build.
// In the original prototype these were embedded as base64 on `window`; here we
// reference the same PNGs directly.

export const mintCapitalLogo = '/mint-capital-logo.png';
export const heroImageDataUrl = '/mint-capital-hero.png';
